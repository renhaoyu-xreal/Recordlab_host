# Recordlab_host

C++ Host infrastructure for the RecordLab refactor MVP.

Build:

```bash
cmake -S /home/hyren/Recordlab_host -B /home/hyren/Recordlab_host/build
cmake --build /home/hyren/Recordlab_host/build
ctest --test-dir /home/hyren/Recordlab_host/build --output-on-failure
```
